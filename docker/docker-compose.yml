version: "3.8"

services:
  honeytrap:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: honeytrap
    restart: unless-stopped
    ports:
      - "2222:2222"   # SSH honeypot
      - "8080:8080"   # HTTP honeypot
      - "8443:8443"   # HTTPS honeypot
      - "2121:2121"   # FTP honeypot
      - "8000:8000"   # API
    environment:
      - HONEYTRAP_ENV=production
      - DATABASE_URL=postgresql+asyncpg://honeytrap:${DB_PASSWORD:-honeytrap}@postgres:5432/honeytrap
      - REDIS_URL=redis://redis:6379/0
      - API_SECRET_KEY=${API_SECRET_KEY:-change-this-in-production}
      - LOG_LEVEL=INFO
      - LOG_FORMAT=json
    volumes:
      - honeytrap_data:/app/data
      - honeytrap_models:/app/ml/models
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - honeytrap_network
    command: ["python", "core/honeypot.py"]

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: honeytrap-api
    restart: unless-stopped
    ports:
      - "8001:8000"
    environment:
      - HONEYTRAP_ENV=production
      - DATABASE_URL=postgresql+asyncpg://honeytrap:${DB_PASSWORD:-honeytrap}@postgres:5432/honeytrap
      - REDIS_URL=redis://redis:6379/0
      - API_SECRET_KEY=${API_SECRET_KEY:-change-this-in-production}
    volumes:
      - honeytrap_data:/app/data:ro
      - honeytrap_models:/app/ml/models:ro
    depends_on:
      - postgres
      - redis
    networks:
      - honeytrap_network

  postgres:
    image: postgres:15-alpine
    container_name: honeytrap-db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=honeytrap
      - POSTGRES_PASSWORD=${DB_PASSWORD:-honeytrap}
      - POSTGRES_DB=honeytrap
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U honeytrap"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - honeytrap_network

  redis:
    image: redis:7-alpine
    container_name: honeytrap-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - honeytrap_network

  dashboard:
    build:
      context: ../dashboard
      dockerfile: Dockerfile
    container_name: honeytrap-dashboard
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - api
    networks:
      - honeytrap_network

  nginx:
    image: nginx:alpine
    container_name: honeytrap-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/nginx/certs:ro
    depends_on:
      - api
      - dashboard
    networks:
      - honeytrap_network

volumes:
  postgres_data:
  redis_data:
  honeytrap_data:
  honeytrap_models:

networks:
  honeytrap_network:
    driver: bridge
